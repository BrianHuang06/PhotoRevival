# 当前数据资料

本目录只保存非图片数据：

- `indexes/`：图片路径、尺寸、哈希和类别索引；
- `labels/`：图片破损标签和纹理素材分类；
- `metadata/`：数据集统计；
- `provenance/`：当前图片的来源、许可、尺寸和哈希。

正式图片文件不进入代码仓库，位于网盘资料包：

```text
D:\My_Docs\Programmes\NCIEP\My_Materials\MarkedImages\dataset_images
```

仓库中的 `export_path` 相对于 `MarkedImages` 根目录解析。
